name: ContributeWordPress
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  ContributeWordPress:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
